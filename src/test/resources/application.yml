#우선권은 test,jdbc:h2:mem:test로 메모리모드로 동작
#스프링부트는 별도의 설정이없으면 메모리모드로 돌아가게됨
#spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace